query getUserData($id: uuid, $withOrders: bool, $withProfile: bool) {
  from(users)
  .where(users.id == $id)
  .select(
    users.*,
    when($withOrders, users.name),
    when($withProfile, users.email)
  )
}