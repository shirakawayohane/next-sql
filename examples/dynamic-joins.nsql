query getUserData($id: uuid, $withOrders: bool, $withProfile: bool) {
  alias u = users
  from(users)
  .where(u.id == $id)
  .select(
    u.*,
    when($withOrders, u.name),
    when($withProfile, u.email)
  )
}