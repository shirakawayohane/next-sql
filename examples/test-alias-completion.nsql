// Test file for alias completion
// Try typing after "alias u = " to see table name suggestions

query testAliasCompletion() {
  // Type after the = to see table suggestions
  alias u = 
  
  // Another alias
  alias p = 
  
  from(users)
  .select(u.*)
}