// AUTO-GENERATED by nextsql. Do not edit.

pub struct AddressId(pub uuid::Uuid);

impl nextsql_backend_rust_runtime::ToSqlParam for AddressId {
    fn as_any(&self) -> &(dyn std::any::Any + Send + Sync) {
        &self.0
    }
}

pub struct CategoryId(pub uuid::Uuid);

impl nextsql_backend_rust_runtime::ToSqlParam for CategoryId {
    fn as_any(&self) -> &(dyn std::any::Any + Send + Sync) {
        &self.0
    }
}

pub struct CustomerId(pub uuid::Uuid);

impl nextsql_backend_rust_runtime::ToSqlParam for CustomerId {
    fn as_any(&self) -> &(dyn std::any::Any + Send + Sync) {
        &self.0
    }
}

pub struct OrderId(pub uuid::Uuid);

impl nextsql_backend_rust_runtime::ToSqlParam for OrderId {
    fn as_any(&self) -> &(dyn std::any::Any + Send + Sync) {
        &self.0
    }
}

pub struct ProductId(pub uuid::Uuid);

impl nextsql_backend_rust_runtime::ToSqlParam for ProductId {
    fn as_any(&self) -> &(dyn std::any::Any + Send + Sync) {
        &self.0
    }
}

pub struct ReviewId(pub uuid::Uuid);

impl nextsql_backend_rust_runtime::ToSqlParam for ReviewId {
    fn as_any(&self) -> &(dyn std::any::Any + Send + Sync) {
        &self.0
    }
}

pub struct CountOrdersByStatusRow {
    pub status: String,
    pub order_count: String,
}

impl CountOrdersByStatusRow {
    fn from_row(row: &dyn nextsql_backend_rust_runtime::Row) -> Self {
        Self {
            status: row.get_string(0),
            order_count: row.get_string(1),
        }
    }
}

pub async fn count_orders_by_status(
    client: &(impl nextsql_backend_rust_runtime::Client + ?Sized),
) -> Result<Vec<CountOrdersByStatusRow>, Box<dyn std::error::Error + Send + Sync>> {
    let rows = client.query(
        "SELECT orders.status, order_count, COUNT(orders.id) AS order_count FROM orders GROUP BY orders.status",
        &[],
    ).await?;
    Ok(rows.iter().map(|row| CountOrdersByStatusRow::from_row(row)).collect())
}

pub struct SalesByCategoryRow {
    pub category_id: CategoryId,
    pub total_revenue: String,
    pub total_items: String,
}

impl SalesByCategoryRow {
    fn from_row(row: &dyn nextsql_backend_rust_runtime::Row) -> Self {
        Self {
            category_id: CategoryId(row.get_uuid(0)),
            total_revenue: row.get_string(1),
            total_items: row.get_string(2),
        }
    }
}

pub async fn sales_by_category(
    client: &(impl nextsql_backend_rust_runtime::Client + ?Sized),
) -> Result<Vec<SalesByCategoryRow>, Box<dyn std::error::Error + Send + Sync>> {
    let rows = client.query(
        "SELECT products.category_id, total_revenue, total_items, SUM(order_items.unit_price * order_items.quantity) AS total_revenue, COUNT(order_items.id) AS total_items FROM order_items INNER JOIN products ON order_items.product_id = products.id GROUP BY products.category_id ORDER BY total_revenue DESC",
        &[],
    ).await?;
    Ok(rows.iter().map(|row| SalesByCategoryRow::from_row(row)).collect())
}

pub struct TopCustomersParams {
    pub min_orders: i64,
}

impl TopCustomersParams {
    fn to_params(&self) -> Vec<&dyn nextsql_backend_rust_runtime::ToSqlParam> {
        vec![]
    }
}

pub struct TopCustomersRow {
    pub customer_id: CustomerId,
    pub order_count: String,
    pub total_spent: String,
}

impl TopCustomersRow {
    fn from_row(row: &dyn nextsql_backend_rust_runtime::Row) -> Self {
        Self {
            customer_id: CustomerId(row.get_uuid(0)),
            order_count: row.get_string(1),
            total_spent: row.get_string(2),
        }
    }
}

pub async fn top_customers(
    client: &(impl nextsql_backend_rust_runtime::Client + ?Sized),
    params: &TopCustomersParams,
) -> Result<Vec<TopCustomersRow>, Box<dyn std::error::Error + Send + Sync>> {
    let rows = client.query(
        "SELECT orders.customer_id, order_count, total_spent, COUNT(orders.id) AS order_count, SUM(orders.total_amount) AS total_spent FROM orders GROUP BY orders.customer_id HAVING COUNT(orders.id) ORDER BY total_spent DESC",
        &params.to_params(),
    ).await?;
    Ok(rows.iter().map(|row| TopCustomersRow::from_row(row)).collect())
}

pub struct AverageRatingByProductRow {
    pub product_id: ProductId,
    pub avg_rating: String,
    pub review_count: String,
}

impl AverageRatingByProductRow {
    fn from_row(row: &dyn nextsql_backend_rust_runtime::Row) -> Self {
        Self {
            product_id: ProductId(row.get_uuid(0)),
            avg_rating: row.get_string(1),
            review_count: row.get_string(2),
        }
    }
}

pub async fn average_rating_by_product(
    client: &(impl nextsql_backend_rust_runtime::Client + ?Sized),
) -> Result<Vec<AverageRatingByProductRow>, Box<dyn std::error::Error + Send + Sync>> {
    let rows = client.query(
        "SELECT reviews.product_id, avg_rating, review_count, AVG(reviews.rating) AS avg_rating, COUNT(reviews.id) AS review_count FROM reviews GROUP BY reviews.product_id HAVING COUNT(reviews.id) ORDER BY avg_rating DESC",
        &[],
    ).await?;
    Ok(rows.iter().map(|row| AverageRatingByProductRow::from_row(row)).collect())
}

pub struct CustomerSpendingRow {
    pub customer_id: CustomerId,
    pub total: String,
    pub item_count: String,
}

impl CustomerSpendingRow {
    fn from_row(row: &dyn nextsql_backend_rust_runtime::Row) -> Self {
        Self {
            customer_id: CustomerId(row.get_uuid(0)),
            total: row.get_string(1),
            item_count: row.get_string(2),
        }
    }
}

pub async fn customer_spending(
    client: &(impl nextsql_backend_rust_runtime::Client + ?Sized),
) -> Result<Vec<CustomerSpendingRow>, Box<dyn std::error::Error + Send + Sync>> {
    let rows = client.query(
        "SELECT orders.customer_id, total, item_count, SUM(order_items.unit_price * order_items.quantity) AS total, COUNT(order_items.id) AS item_count FROM orders INNER JOIN order_items ON orders.id = order_items.order_id WHERE orders.status != 'cancelled' GROUP BY orders.customer_id ORDER BY total DESC LIMIT 100",
        &[],
    ).await?;
    Ok(rows.iter().map(|row| CustomerSpendingRow::from_row(row)).collect())
}

pub struct PriceRangeByCategoryRow {
    pub category_id: CategoryId,
    pub min_price: String,
    pub max_price: String,
    pub product_count: String,
}

impl PriceRangeByCategoryRow {
    fn from_row(row: &dyn nextsql_backend_rust_runtime::Row) -> Self {
        Self {
            category_id: CategoryId(row.get_uuid(0)),
            min_price: row.get_string(1),
            max_price: row.get_string(2),
            product_count: row.get_string(3),
        }
    }
}

pub async fn price_range_by_category(
    client: &(impl nextsql_backend_rust_runtime::Client + ?Sized),
) -> Result<Vec<PriceRangeByCategoryRow>, Box<dyn std::error::Error + Send + Sync>> {
    let rows = client.query(
        "SELECT products.category_id, min_price, max_price, product_count, MIN(products.price) AS min_price, MAX(products.price) AS max_price, COUNT(products.id) AS product_count FROM products WHERE products.is_active = TRUE GROUP BY products.category_id",
        &[],
    ).await?;
    Ok(rows.iter().map(|row| PriceRangeByCategoryRow::from_row(row)).collect())
}

pub struct ActiveCustomerCountRow {
    pub total: String,
}

impl ActiveCustomerCountRow {
    fn from_row(row: &dyn nextsql_backend_rust_runtime::Row) -> Self {
        Self {
            total: row.get_string(0),
        }
    }
}

pub async fn active_customer_count(
    client: &(impl nextsql_backend_rust_runtime::Client + ?Sized),
) -> Result<Vec<ActiveCustomerCountRow>, Box<dyn std::error::Error + Send + Sync>> {
    let rows = client.query(
        "SELECT total, COUNT(customers.id) AS total FROM customers WHERE customers.is_active = TRUE",
        &[],
    ).await?;
    Ok(rows.iter().map(|row| ActiveCustomerCountRow::from_row(row)).collect())
}

pub struct HighValueCustomersParams {
    pub min_total: f64,
}

impl HighValueCustomersParams {
    fn to_params(&self) -> Vec<&dyn nextsql_backend_rust_runtime::ToSqlParam> {
        vec![]
    }
}

pub struct HighValueCustomersRow {
    pub customer_id: CustomerId,
    pub total_spent: String,
    pub order_count: String,
}

impl HighValueCustomersRow {
    fn from_row(row: &dyn nextsql_backend_rust_runtime::Row) -> Self {
        Self {
            customer_id: CustomerId(row.get_uuid(0)),
            total_spent: row.get_string(1),
            order_count: row.get_string(2),
        }
    }
}

pub async fn high_value_customers(
    client: &(impl nextsql_backend_rust_runtime::Client + ?Sized),
    params: &HighValueCustomersParams,
) -> Result<Vec<HighValueCustomersRow>, Box<dyn std::error::Error + Send + Sync>> {
    let rows = client.query(
        "SELECT orders.customer_id, total_spent, order_count, SUM(orders.total_amount) AS total_spent, COUNT(orders.id) AS order_count FROM orders WHERE orders.status != 'cancelled' GROUP BY orders.customer_id HAVING SUM(orders.total_amount) ORDER BY total_spent DESC",
        &params.to_params(),
    ).await?;
    Ok(rows.iter().map(|row| HighValueCustomersRow::from_row(row)).collect())
}

