mutation deleteUser($id: uuid) {
  delete(users)
  .where(exists(
    $(
      from(user_action_logs)
      .where(
        user_action_logs.user_id == users.id && user_action_logs.deleted == true
      )
      .select(*)
    )
  ))
}
